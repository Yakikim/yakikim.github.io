---
pagination:
  data: collections
  size: 1
  alias: tag
  filter:
    - all
    - nav
    - post
    - posts
    - tagList
    - APEX
  addAllPagesToCollections: true
layout: layouts/home.njk
eleventyComputed:
  title: Tagged “{{ tag }}”
  header: 'Tagged: “#{{ tag }}”'
permalink: /tags/{{ tag | slug }}/
---

{% set postslist = collections[ tag ] %}

{% include "componnents/postslist.njk" %}

<p>See <a href="{{ '/tags/' | url }}">all tags</a>.</p>
